services:
  answer:
    build:
      context: .
      dockerfile: Dockerfile.multisite
    ports:
      - '9080:80'
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: on-failure
    volumes:
      - answer-data:/data

volumes:
  answer-data:

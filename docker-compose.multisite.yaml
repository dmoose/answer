services:
  answer:
    build:
      context: .
      dockerfile: Dockerfile.multisite
    ports:
      - '9080:80'
    restart: on-failure
    volumes:
      - answer-data:/data

volumes:
  answer-data:
